// Ternary operator

var age = 18;

var type;

if(age >= 18){
    type = "adult";
} else{
    type = "child"
}
console.log(type);





// var age = 61;

// var eligible;
// if (age > 60) {
//     eligible = 'Yes, is eligible to get pension.';
// } else{
//     eligible = 'No , is not eligible to get Pension.';
// }
// console.log(eligible);

var old = 30;
var eligibility = (old >= 60) ? "Yes , he is eligible to get Pension."
                                : "No, he isn't eligible to get Pension.";
                             
console.log(eligibility);

// by ternary operator it's so much simple

// var ternary = (if condition) ? returning value of if : else value;

var year = 19;
var look = (age >= 20) ? "adult" : "child";


console.log(look);